"""Отчёт по лаб. 2 — Стилевое оформление WEB-страниц (CSS, 6 заданий)."""

from __future__ import annotations

import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT / "scripts"))

from report_builder import (  # noqa: E402
    LabMeta,
    add_bullets,
    add_heading,
    add_image,
    add_listing,
    add_page_break,
    add_para,
    add_qa,
    add_table_simple,
    add_title_page,
    make_doc,
    save,
)

LAB_DIR = ROOT / "2"
SHOTS = ROOT / "docs" / "screenshots" / "lab_02"
SNIPS = ROOT / "docs" / "snippets" / "lab_02"


def read_css(name: str) -> str:
    return (LAB_DIR / "css" / name).read_text(encoding="utf-8")


def main() -> None:
    meta = LabMeta(
        number=2,
        title="Стилевое оформление WEB-страниц средствами CSS",
    )
    doc = make_doc()
    add_title_page(doc, meta)
    add_page_break(doc)

    # === Тема и цель ===
    add_heading(doc, "Тема и цель работы")
    add_para(
        doc,
        "Тема: каскадные таблицы стилей (CSS) — способы подключения "
        "стилей к HTML-документу, основные группы селекторов, "
        "стилизация ссылок-кнопок, таблиц, информационных блоков, "
        "форм регистрации и навигационных меню; базовая адаптивность "
        "через @media-запросы.",
    )
    add_para(
        doc,
        "Цель работы: освоить структуру CSS-правил и приоритет "
        "селекторов; научиться применять CSS для графического "
        "оформления элементов страницы — управления цветом, шрифтом, "
        "отступами, рамками, фоном, размерами; познакомиться с "
        "псевдоклассами (:hover, :focus, :nth-child, :checked) и "
        "псевдоэлементами (::before, ::after); реализовать "
        "стилизованные интерфейсные компоненты и базовую адаптивность.",
    )

    # === Условие задания ===
    add_heading(doc, "Условие задания", level=2)
    add_para(
        doc,
        "Согласно методическим указаниям к лабораторной работе №2, "
        "требуется выполнить 6 индивидуальных заданий, каждое из "
        "которых демонстрирует определённую группу CSS-возможностей:",
    )
    add_bullets(
        doc,
        [
            "Задание 1. Реализовать пример применения всех "
            "основных групп селекторов CSS (элемента, класса, "
            "идентификатора, потомка, атрибута, псевдокласса, "
            "псевдоэлемента).",
            "Задание 2. Создать набор стилизованных кнопок-ссылок "
            "(<a class=\"btn\">) с различными эффектами оформления "
            "и поведением при наведении.",
            "Задание 3. Стилизовать таблицу 7×4 «Список сотрудников "
            "отдела» — заголовки столбцов и строк, чередующиеся фоны "
            "строк, разделители столбцов, подсветка строки при "
            "наведении.",
            "Задание 4. Создать карточку-анонс в стиле «Дзен» (имя "
            "автора, заголовок, краткое описание, ссылка «Читать "
            "полностью») и страницу полной статьи со своей "
            "типографикой.",
            "Задание 5. Реализовать адаптивную форму регистрации "
            "(поля имени/фамилии, email, пароля, кнопок) с плавными "
            "анимациями фокуса и валидацией.",
            "Задание 6. Построить страницу с двумя меню навигации — "
            "вертикальным (фиксированной шириной) и горизонтальным "
            "(sticky-header). Адаптация для мобильных устройств.",
        ],
    )

    # === Краткая теория ===
    add_page_break(doc)
    add_heading(doc, "Краткая теория")

    add_heading(doc, "Способы подключения CSS", level=2)
    add_para(
        doc,
        "Существует три способа подключения CSS-стилей к "
        "HTML-документу, перечисленных в порядке убывания "
        "приоритета (при одинаковой специфичности правил):",
    )
    add_table_simple(
        doc,
        [
            ["Способ", "Синтаксис", "Когда применять"],
            ["Внешний файл", "<link rel=\"stylesheet\" href=\"...\">", "Основной способ. Кэшируется браузером, переиспользуется."],
            ["Внутренние <style>", "<style>p { ... }</style> в <head>", "Для small/medium проектов или специфичных стилей одной страницы."],
            ["Inline", "<p style=\"color:red\">", "Только для динамически генерируемых стилей (JS)."],
        ],
        caption="Таблица 1 — способы подключения CSS",
    )
    add_para(
        doc,
        "В данной лабораторной работе используется первый, "
        "рекомендуемый способ: общие правила (CSS-переменные, "
        "сброс отступов) вынесены в css/common.css, а у каждого "
        "задания — собственный CSS-файл с именем "
        "css/0X-название.css. Это позволяет переиспользовать "
        "общие переменные между заданиями и одновременно "
        "изолировать стили каждой страницы.",
    )

    add_heading(doc, "Основные группы CSS-селекторов", level=2)
    add_table_simple(
        doc,
        [
            ["Группа", "Пример", "Что выбирает"],
            ["Элемента", "p { ... }", "Все элементы <p>"],
            ["Класса", ".lead { ... }", "Элементы с class=\"lead\""],
            ["Идентификатора", "#article", "Элемент с id=\"article\""],
            ["Потомка", ".card h3", "Все <h3> внутри элементов .card"],
            ["Дочерний", ".menu > li", "Только прямые потомки <li>"],
            ["Атрибута", "a[target=\"_blank\"]", "Ссылки с target=\"_blank\""],
            ["Псевдокласс", "a:hover, :nth-child(odd)", "Состояния, позиции, выбор"],
            ["Псевдоэлемент", "::before, ::first-letter", "Виртуальные фрагменты"],
        ],
        caption="Таблица 2 — группы CSS-селекторов",
    )

    add_heading(doc, "Box-model и приоритет (специфичность)", level=2)
    add_para(
        doc,
        "Каждый элемент в CSS представляется как блок (box-model) с "
        "четырьмя областями вокруг содержимого: padding "
        "(внутренний отступ), border (рамка), margin (внешний "
        "отступ). Размер блока по умолчанию рассчитывается как "
        "width + padding + border; для удобства верстальщики "
        "обычно переключают расчёт на box-sizing: border-box, "
        "при котором ширина включает padding и border.",
    )
    add_para(
        doc,
        "Приоритет правил при конфликте определяется через "
        "специфичность (a, b, c): a — inline (1, 0, 0, 0), b — "
        "число id-селекторов, c — число class/attribute/"
        "pseudo-class, d — число tag/pseudo-element. Чем выше "
        "специфичность, тем выше приоритет. При равной "
        "специфичности побеждает правило, объявленное позднее.",
    )

    # === ПО ЗАДАНИЯМ ===
    tasks = [
        {
            "n": 1,
            "title": "Все типы CSS-селекторов",
            "summary": (
                "На одной странице последовательно применены все семь "
                "групп селекторов: селектор элемента (p), класса "
                "(.lead), идентификатора (#hero), потомка (.card "
                "h3), атрибута (a[target=\"_blank\"]), псевдокласса "
                "(:hover, :nth-child) и псевдоэлемента (::first-letter, "
                "::after). Каждое правило применено к видимому "
                "элементу страницы — эффект каждого селектора "
                "наблюдается невооружённым глазом."
            ),
            "shot": "01_selectors.png",
            "snip_parts": ["01_selectors_a.png", "01_selectors_b.png"],
        },
        {
            "n": 2,
            "title": "Стилизованные кнопки-ссылки",
            "summary": (
                "Реализованы три варианта оформления ссылок-кнопок: "
                "(а) .btn-primary — сплошной фиолетовый фон с "
                "контрастным белым текстом, эффект «нажатия» через "
                ":active; (б) .btn-shadow — оранжевый градиент с "
                "выраженной тенью box-shadow (как в примере из "
                "PDF), увеличение scale(1.05) при наведении, "
                "стрелка через псевдоэлемент ::after; (в) "
                ".btn-ghost — контурный вариант с подчёркиванием "
                "через ::before, заливающийся при :hover."
            ),
            "shot": "02_buttons.png",
            "snip_parts": ["02_buttons_a.png", "02_buttons_b.png"],
        },
        {
            "n": 3,
            "title": "Стилизованная таблица 7×4",
            "summary": (
                "Таблица «Список сотрудников отдела» оформлена "
                "следующим образом: thead th — градиентная шапка; "
                "tbody th — лёгкий фон заголовков строк; "
                ":nth-child(odd)/(even) — чередующиеся фоны строк; "
                "td + td — вертикальные разделители столбцов через "
                "border-left; tr:hover — подсветка строки при "
                "наведении; курсор pointer; border-radius + "
                "overflow:hidden для скруглённых углов; box-shadow "
                "для визуальной глубины."
            ),
            "shot": "03_table.png",
            "snip_parts": ["03_table_a.png", "03_table_b.png"],
        },
        {
            "n": 4,
            "title": "Информационный блок (Дзен-стиль)",
            "summary": (
                "Карточка-анонс статьи в стиле сервиса «Яндекс.Дзен»: "
                "крупное изображение (inline-SVG горный пейзаж "
                "с градиентом), под ним — аватар автора, имя автора, "
                "заголовок статьи, краткое описание (превью) и "
                "ссылка-кнопка «Читать полностью». По клику на "
                "ссылку открывается страница полной статьи "
                "04-article.html со своей типографикой (шрифт "
                "Georgia, увеличенный line-height, оформленная "
                "цитата blockquote)."
            ),
            "shot": "04_info_block.png",
            "snip_parts": ["04_info_block_a.png", "04_info_block_b.png"],
            "extra_shot": "04_article.png",
            "extra_snip_parts": ["04_article_a.png", "04_article_b.png"],
        },
        {
            "n": 5,
            "title": "Адаптивная форма регистрации",
            "summary": (
                "Форма содержит поля: имя, фамилия, email, пароль, "
                "подтверждение пароля; кнопки «Регистрация», "
                "«Войти», «Забыли пароль?». Особенности: "
                "обязательные поля помечены красной звёздочкой; "
                "поведение валидации реализовано через "
                ":invalid:not(:placeholder-shown) — ошибка не "
                "показывается на пустом поле, но появляется при "
                "вводе некорректного значения; плавные transitions "
                "на :focus; градиентный фон на всём экране. "
                "Адаптивность: при ширине < 560px поля «Имя/"
                "Фамилия» и «Пароль/Подтверждение» становятся в "
                "один столбец через @media-запрос."
            ),
            "shot": "05_registration.png",
            "snip_parts": ["05_registration_a.png", "05_registration_b.png"],
        },
        {
            "n": 6,
            "title": "Меню навигации (вертикальное + горизонтальное)",
            "summary": (
                "Страница содержит два меню одновременно: слева — "
                "вертикальное меню position: fixed шириной 200px и "
                "высотой 100vh с подсветкой пунктов при :hover и "
                "заранее выделенным активным пунктом; сверху — "
                "горизонтальное sticky-меню с CTA-кнопкой справа. "
                "На экранах < 720px вертикальное меню превращается "
                "в горизонтальную ленту наверху страницы через "
                "@media-запрос."
            ),
            "shot": "06_menu.png",
            "snip_parts": ["06_menu_a.png", "06_menu_b.png"],
        },
    ]

    rfig = 1  # глобальный счётчик рисунков
    lst_n = 1  # глобальный счётчик листингов
    for t in tasks:
        add_page_break(doc)
        add_heading(doc, f"Задание №{t['n']}. {t['title']}")
        add_para(doc, t["summary"])
        rfig += 1
        add_image(
            doc,
            SHOTS / t["shot"],
            caption=f"Рисунок {rfig} — внешний вид (задание {t['n']})",
        )
        for idx, part in enumerate(t["snip_parts"], start=1):
            lst_n += 1
            add_image(
                doc,
                SNIPS / part,
                caption=(
                    f"Листинг {lst_n} — CSS задания {t['n']} "
                    f"(часть {idx} из {len(t['snip_parts'])})"
                ),
            )
        # Доп. экран и сниппеты (для задания 4 — отдельная страница статьи)
        if "extra_shot" in t:
            rfig += 1
            add_image(
                doc,
                SHOTS / t["extra_shot"],
                caption=f"Рисунок {rfig} — страница полной статьи",
            )
            for idx, part in enumerate(t["extra_snip_parts"], start=1):
                lst_n += 1
                add_image(
                    doc,
                    SNIPS / part,
                    caption=(
                        f"Листинг {lst_n} — CSS страницы статьи "
                        f"(часть {idx} из {len(t['extra_snip_parts'])})"
                    ),
                )

    # === Проверка ===
    add_page_break(doc)
    add_heading(doc, "Проверка корректности страниц")
    add_para(
        doc,
        "Запуск локального HTTP-сервера и опрос всех 8 страниц "
        "(index + 7 заданий с учётом отдельной страницы статьи "
        "из задания 4):",
    )
    add_listing(
        doc,
        "python3 -m http.server 8766 --bind 127.0.0.1 &\n"
        "for f in index 01-selectors 02-buttons 03-table \\\n"
        "         04-info-block 04-article 05-registration 06-menu; do\n"
        "    curl -s -o /dev/null -w \"%{http_code} $f.html\\n\" \\\n"
        "         \"http://127.0.0.1:8766/$f.html\"\n"
        "done",
        caption=f"Листинг {lst_n + 1} — скрипт проверки",
    )
    add_para(
        doc,
        "Все 8 страниц и все 8 CSS-файлов (включая common.css) "
        "возвращают код 200 OK. Все ожидаемые визуальные эффекты "
        "(hover, переходы, адаптивность) проверены вручную в "
        "браузере Chromium через headless Playwright.",
    )

    # === Контрольные вопросы ===
    add_page_break(doc)
    add_heading(doc, "Контрольные вопросы")
    QA = [
        (
            "Чем отличается псевдокласс от псевдоэлемента?",
            "Псевдокласс описывает состояние или позицию реально "
            "существующего элемента (:hover — наведение, :focus — "
            "фокус, :nth-child(2n) — каждый второй среди соседей, "
            ":checked — отмеченный чекбокс). Псевдоэлемент создаёт "
            "виртуальный, не существующий в DOM фрагмент (::before "
            "и ::after добавляют контент до/после, ::first-letter "
            "стилизует первую букву, ::placeholder — текст-подсказку "
            "в input). Синтаксически они различаются числом "
            "двоеточий: один (:) для псевдокласса, два (::) для "
            "псевдоэлемента (хотя CSS 2.1 псевдоэлементы с одним "
            "двоеточием по-прежнему поддерживаются для обратной "
            "совместимости).",
        ),
        (
            "Как рассчитывается специфичность CSS-селектора?",
            "Специфичность представляется в виде кортежа "
            "(a, b, c, d), где a — число inline-стилей (1 или 0), "
            "b — число идентификаторов в селекторе, c — число "
            "классов / атрибутов / псевдоклассов, d — число имён "
            "тегов / псевдоэлементов. Кортежи сравниваются "
            "лексикографически: (0, 1, 0, 0) > (0, 0, 99, 99). При "
            "равной специфичности побеждает правило, объявленное "
            "позднее. Универсальный селектор * не добавляет "
            "специфичности. Селектор :not(...) не учитывается сам, "
            "но содержимое внутри — учитывается. Объявление "
            "!important пересиливает обычную специфичность; этим "
            "следует пользоваться минимально.",
        ),
        (
            "Что такое box-model и зачем нужен box-sizing: border-box?",
            "Box-model — модель блочного представления элемента в "
            "CSS: центральная область с шириной width и высотой "
            "height, окружённая padding (внутренний отступ), border "
            "(рамка) и margin (внешний отступ). По умолчанию "
            "(content-box) свойства width/height задают размер "
            "только content, и padding с border добавляются сверху "
            "— фактический размер блока больше width. Это неудобно "
            "при вёрстке сеток. border-box делает width/height "
            "включать padding + border, так что заданный width "
            "становится фактическим внешним размером. Принято "
            "глобально подключать * { box-sizing: border-box; }.",
        ),
        (
            "Чем :hover отличается от :focus и :active?",
            ":hover — состояние «курсор находится над элементом». "
            "Срабатывает только при наличии мыши; на сенсорных "
            "экранах поведение нестабильно. :focus — состояние "
            "«элемент имеет фокус ввода» (после клика или "
            "Tab-навигации); важно для интерактивных элементов с "
            "клавиатуры. :active — состояние «кнопка нажата, но не "
            "отпущена» (между mousedown и mouseup). Современная "
            "практика — стилизовать оба :hover и :focus одинаково "
            "(:hover, :focus { ... }) для одинакового поведения с "
            "мышью и клавиатурой; для тач-устройств — "
            ":focus-visible.",
        ),
        (
            "Как реализовать чередующийся фон строк таблицы?",
            "Через псевдокласс :nth-child(odd) или :nth-child(even). "
            "Например: tbody tr:nth-child(odd) { background: "
            "#f3f3f3; } чередует фон. Можно использовать формулу "
            "An+B: :nth-child(3n) — каждая третья, :nth-child(3n+1) "
            "— каждая первая в группах по три. В современных "
            "браузерах также поддерживается селектор :nth-of-type "
            "— считающий только среди соседей того же тега.",
        ),
        (
            "Что делает @media-запрос и как реализовать mobile-first?",
            "@media-запрос условно применяет CSS-правила в "
            "зависимости от характеристик устройства: ширины окна "
            "(min-width / max-width), плотности пикселей "
            "(min-resolution), ориентации, цветовой схемы "
            "(prefers-color-scheme). Подход mobile-first означает: "
            "базовые правила (вне @media) написаны для мобильных "
            "устройств (узкий экран), а @media (min-width: ...) "
            "добавляет расширения для широких экранов. Преимущество: "
            "проще код (меньше переопределений), меньшие CSS-файлы "
            "для мобильных устройств, лучшая производительность.",
        ),
    ]
    for i, (q, a) in enumerate(QA, start=1):
        add_qa(doc, i, q, a)

    # === Выводы ===
    add_page_break(doc)
    add_heading(doc, "Выводы")
    add_para(
        doc,
        "В работе реализованы все 6 заданий, демонстрирующих "
        "основные возможности CSS: 7 групп селекторов, стилизация "
        "интерактивных кнопок-ссылок с эффектами наведения, "
        "оформление таблицы с чередующимися строками и подсветкой, "
        "создание карточек-анонсов в стиле «Дзен» с отдельной "
        "страницей полной статьи, адаптивная форма регистрации с "
        "валидацией через :invalid:not(:placeholder-shown), "
        "сложная компоновка из вертикального и горизонтального "
        "меню с адаптивной перестройкой под мобильные устройства.",
    )
    add_para(
        doc,
        "На каждой странице явно использованы соответствующие "
        "селекторы и CSS-свойства; HTML-разметка осталась "
        "минимальной и семантичной — всё оформление вынесено в "
        "css/. Адаптивность реализована в заданиях 5 (форма "
        "регистрации) и 6 (двойное меню) через @media-запросы. "
        "Локальный HTTP-сервер подтверждает доступность всех 8 "
        "HTML-страниц и 8 CSS-файлов (включая общий common.css).",
    )

    # === Полные листинги ===
    add_page_break(doc)
    add_heading(doc, "Полные текстовые листинги CSS")
    add_para(
        doc,
        "Ниже приведены полные исходники CSS-файлов в текстовой "
        "форме — для удобства копирования преподавателем.",
    )
    css_files = [
        "common.css",
        "01-selectors.css",
        "02-buttons.css",
        "03-table.css",
        "04-info-block.css",
        "04-article.css",
        "05-registration.css",
        "06-menu.css",
    ]
    for fname in css_files:
        lst_n += 1
        add_listing(
            doc,
            read_css(fname),
            caption=f"Листинг {lst_n} — css/{fname}",
        )

    out = ROOT / "docs" / "reports" / "lab_02" / "Ковалев Д.П. ВКБ43 2 лаба.docx"
    save(doc, out)
    print(f"saved: {out}")


if __name__ == "__main__":
    main()
