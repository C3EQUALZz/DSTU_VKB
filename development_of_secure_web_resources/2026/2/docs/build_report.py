"""Создание отчёта на основе оформления предоставленной ЛР1."""
from pathlib import Path
import platform
from docx import Document
from docx.shared import Pt
from docx.enum.text import WD_ALIGN_PARAGRAPH

ROOT = Path(__file__).resolve().parent.parent
TEMPLATE = ROOT.parent / '1/docs/Ковалев Д.П. ВКБ53 ЛР1.docx'
doc = Document(TEMPLATE)
# Сохраняем титульный лист, стили, поля страницы и колонтитулы образца.
for element in list(doc.element.body)[18:]:
    if not element.tag.endswith('sectPr'):
        doc.element.body.remove(element)
doc.paragraphs[8].runs[0].text = 'Лабораторная работа № 2'
p = doc.paragraphs[10]
p.clear()
p.add_run('на тему «Введение в объектно-ориентированное программирование. Классы и объекты»')
doc.core_properties.title = 'Лабораторная работа № 2. Классы и объекты'
doc.core_properties.author = 'Ковалев Данил Петрович'
doc.core_properties.subject = 'Разработка защищённых Web-ресурсов'


def heading(text):
    p = doc.add_paragraph(text, 'Heading 1')
    p.paragraph_format.page_break_before = True


def sub(text):
    doc.add_paragraph(text, 'Heading 2')


def para(text):
    p = doc.add_paragraph(text)
    p.alignment = WD_ALIGN_PARAGRAPH.JUSTIFY
    p.paragraph_format.space_after = Pt(6)
    p.paragraph_format.line_spacing = 1.15
    return p


def code(text, caption):
    p = doc.add_paragraph()
    p.paragraph_format.line_spacing = 1
    p.paragraph_format.space_after = Pt(4)
    p.paragraph_format.first_line_indent = Pt(0)
    r = p.add_run(text.rstrip())
    r.font.name = 'Courier New'
    r.font.size = Pt(9)
    p = doc.add_paragraph(caption)
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    p.paragraph_format.space_after = Pt(8)
    for r in p.runs:
        r.font.size = Pt(11)
        r.italic = True


def table(headers, rows):
    t = doc.add_table(rows=1, cols=len(headers))
    t.style = 'Table Grid'
    for c, text in zip(t.rows[0].cells, headers): c.text = text
    for row in rows:
        for c, text in zip(t.add_row().cells, row): c.text = text
    for row in t.rows:
        for cell in row.cells:
            for p in cell.paragraphs:
                p.paragraph_format.space_after = Pt(3)
                p.paragraph_format.line_spacing = 1
                for r in p.runs: r.font.size = Pt(11)
    for c in t.rows[0].cells:
        for r in c.paragraphs[0].runs: r.bold = True


heading('1. Тема, цель и условие задания')
para('Тема: введение в объектно-ориентированное программирование; классы, объекты, атрибуты и методы в Python.')
para('Цель работы: научиться описывать состояние объектов в инициализаторе, изменять его методами, соблюдать ограничения предметной модели и разграничивать атрибуты класса и экземпляра.')
para('По методическим указаниям необходимо дополнить два исходных файла. В main.py реализуется класс BacteriaProducer, управляющий колонией бактерий. В task2.py завершается класс MushroomsCollector, проверяющий и сохраняющий грибы в отдельных корзинах.')
sub('Требования к результату')
para('Колония создаётся пустой и имеет заданную вместимость max_bacteria. Метод create() добавляет одну бактерию, delete() удаляет одну. Выход за границы от нуля до вместимости запрещён; успешные действия и отказы сопровождаются сообщениями из задания.')
para('Каждая корзина должна иметь собственный список mushrooms. Метод is_poisonous() определяет принадлежность названия к двум запрещённым значениям из прекода; add_mushroom() добавляет допустимый гриб или печатает сообщение об отказе. Метод __str__() возвращает содержимое через запятую и пробел.')
sub('Основные понятия')
table(['Понятие', 'Применение в работе'], [
('Класс', 'Описание данных и действий: BacteriaProducer, MushroomsCollector.'),
('Объект', 'Отдельная колония или корзина со своим состоянием.'),
('Инициализатор __init__', 'Задаёт исходные атрибуты после создания экземпляра.'),
('self', 'Ссылка на экземпляр, для которого вызван метод.'),
('Атрибут экземпляра', 'self.mushrooms: отдельный список каждой корзины.'),
('Атрибут класса', 'Значение на уровне класса; изменяемый список здесь был бы общим.'),
('Метод объекта', 'Действие над конкретной колонией или корзиной.')])

heading('2. Задание № 1. Реализация колонии бактерий')
para('В инициализатор передаётся вместимость колонии. Она сохраняется в self.max_bacteria; счётчик self.current_bacteria_count получает значение 0. Оба атрибута принадлежат экземпляру, поэтому создание второй колонии не изменяет первую.')
code((ROOT / 'main.py').read_text().split("if __name__")[0], 'Листинг 1 — класс BacteriaProducer (main.py)')
para('Метод create() сначала проверяет верхнюю границу. Условие >= включает требуемый случай равенства пределу. При отказе счётчик не меняется. В допустимом случае оператор += увеличивает его ровно на единицу, затем выводится новое значение.')
para('Метод delete() сначала проверяет, пуста ли колония. При нуле выводится отказ без изменения состояния. Иначе оператор -= уменьшает счётчик на единицу. В обоих методах f-строка подставляет текущее количество в сообщение.')
para('Вместимость по смыслу задания — неотрицательное целое число. Валидация других типов и отрицательных значений в условии не задана и в учебный интерфейс не добавлена. Гарантия границ относится к работе через методы при такой вместимости и без внешнего изменения атрибутов.')

heading('3. Задание № 1. Запуск и анализ результата')
para('Пример самопроверки из исходного файла сохранён. Он помещён под условие if __name__ == "__main__", чтобы запускать демонстрацию при выполнении файла и не печатать её при импорте класса в тесты.')
code('if __name__' + (ROOT / 'main.py').read_text().split('if __name__')[1], 'Листинг 2 — демонстрационный сценарий main.py')
import subprocess, sys
out1 = subprocess.check_output([sys.executable, str(ROOT / 'main.py')], text=True)
code('$ python3 main.py\n' + out1, 'Листинг 3 — фактический вывод первого задания')
table(['Шаг', 'Действие', 'До → после', 'Результат'], [
('1', 'delete()', '0 → 0', 'Отказ: пустая колония'),
('2', 'create()', '0 → 1', 'Добавление'),
('3', 'create()', '1 → 2', 'Добавление'),
('4', 'create()', '2 → 3', 'Добавление'),
('5', 'create()', '3 → 3', 'Отказ: достигнут предел'),
('6', 'delete()', '3 → 2', 'Удаление')])
sub('Почему сохраняются границы')
para('Обозначим текущее количество через n, вместимость через M. Изначально n = 0. При n < M добавление даёт n + 1 ≤ M, а при n ≥ M изменения нет. При n > 0 удаление даёт n − 1 ≥ 0, а при n = 0 изменения нет. Следовательно, после любой последовательности вызовов сохраняется 0 ≤ n ≤ M.')

heading('4. Задание № 2. Реализация корзины грибов')
para('В исходном классе список mushrooms был объявлен вне методов. Исправление состоит в создании self.mushrooms = [] внутри __init__. Каждый вызов инициализатора создаёт новый список, связанный только с соответствующим объектом.')
code((ROOT / 'task2.py').read_text().split("if __name__")[0], 'Листинг 4 — класс MushroomsCollector (task2.py)')
sub('Проверка и добавление')
para('Оператор in проверяет, совпадает ли mushroom_name с одним из элементов кортежа ("Мухомор", "Поганка"). Результат — логическое значение True или False. Метод не меняет содержимое корзины.')
para('Метод add_mushroom() вызывает is_poisonous(). При True печатает точно заданное сообщение «Нельзя добавить ядовитый гриб». При False добавляет строку в конец списка методом append(). Порядок находок сохраняется; повторяющиеся названия допустимы, поскольку каждый найденный гриб учитывается отдельно.')
sub('Строковое представление')
para('Метод __str__() возвращает строку, а не печатает её самостоятельно. Выражение ", ".join(self.mushrooms) объединяет названия с разделителем «запятая и пробел». print(collector) использует это представление. Для пустой корзины возвращается пустая строка, для одного гриба — только его название.')
para('Используется точное сравнение названий с учётом регистра, как в исходном примере. Нормализация ввода и полный справочник грибов заданием не предусмотрены. Это учебная проверка двух строк, а не определение съедобности реальных грибов.')

heading('5. Задание № 2. Ошибки прекода и проверка')
sub('Ошибка логического выражения')
code("if mushroom_name == 'Мухомор' or 'Поганка':\n    return True", 'Листинг 5 — ошибочное условие из исходного файла')
para('Выражение интерпретируется как (mushroom_name == "Мухомор") or "Поганка". Непустая строка истинна в логическом контексте. Поэтому при несовпадении с «Мухомор» правая часть всё равно делает условие истинным, и любой гриб считался ядовитым. Исправленный оператор in явно сравнивает входное значение с обоими названиями.')
sub('Ошибка общего изменяемого состояния')
para('При mushrooms = [] в теле класса обращение self.mushrooms находило общий список класса. Вызов append() изменял этот список, поэтому находки одной корзины появлялись в остальных. Присваивание self.mushrooms = [] в __init__ создаёт отдельный атрибут экземпляра и отдельный список.')
code('if __name__' + (ROOT / 'task2.py').read_text().split('if __name__')[1], 'Листинг 6 — демонстрационный сценарий task2.py')
out2 = subprocess.check_output([sys.executable, str(ROOT / 'task2.py')], text=True)
code('$ python3 task2.py\n' + out2, 'Листинг 7 — фактический вывод второго задания')
para('«Мухомор» отклонён. Первая корзина содержит «Подосиновик, Белый». После добавления «Лисичка» во вторую корзину повторный вывод первой остаётся прежним. Это демонстрирует независимость объектов; в тесте также проверено, что их списки не являются одним объектом.')

heading('6. Окружение, структура и воспроизведение')
para('Решение выполнено на Python ' + platform.python_version() + '. Для программ и тестов достаточно стандартной библиотеки Python; установка сторонних пакетов и запуск веб-сервера не требуются. Пути ниже указаны относительно каталога лабораторной работы № 2.')
code('main.py                 # колония бактерий\ntask2.py                # корзина грибов\ntest_lab2.py            # автоматические проверки\nREADME.md               # инструкция запуска\ndocs/verification.txt   # фактический протокол\ndocs/build_report.py    # сборка отчёта DOCX\ndocs/Ковалев Д.П. ВКБ53 ЛР2.docx', 'Листинг 8 — структура подготовленных материалов')
sub('Порядок запуска')
para('Открыть терминал в каталоге с main.py и task2.py. Последовательно выполнить приведённые команды. Первые две запускают исходные демонстрации; третья выполняет тесты и выводит результаты каждого сценария.')
code('python3 main.py\npython3 task2.py\npython3 -m unittest -v test_lab2', 'Листинг 9 — команды воспроизведения')
para('На системе, где интерпретатор доступен под именем python, используется это имя вместо python3. Программы не запрашивают ввод: параметры и последовательность действий заданы в демонстрационных блоках. Для другого сценария можно импортировать класс и вызвать его методы.')
code('from main import BacteriaProducer\nfrom task2 import MushroomsCollector\n\ncolony = BacteriaProducer(max_bacteria=1)\ncolony.create()\ncolony.delete()\n\nbasket = MushroomsCollector()\nbasket.add_mushroom("Белый")\nprint(basket)', 'Листинг 10 — пример использования классов из другого модуля')
para('Файл docs/verification.txt содержит вывод реально выполненных команд, версию Python и коды завершения. Сборка DOCX вынесена в отдельный вспомогательный скрипт и использует python-docx и образец отчёта ЛР1; эти зависимости не нужны для выполнения заданий.')

heading('7. Автоматическая проверка результата')
para('Проверки реализованы в test_lab2.py на unittest. Для сравнения сообщений redirect_stdout направляет вывод в StringIO. Проверяется не только состояние объектов, но и точный текст, включая завершающий перевод строки. Все 12 тестов завершились успешно.')
table(['Тест / группа', 'Проверяемое поведение'], [
('Начальное состояние', 'Вместимость сохраняется; счётчик равен нулю.'),
('Добавление и предел', 'Увеличение на 1; отказ при заполнении; точные сообщения.'),
('Удаление и пустая колония', 'Уменьшение на 1; отказ при нуле; точные сообщения.'),
('Нулевая вместимость', 'Добавление запрещено, счётчик остаётся нулевым.'),
('Независимость колоний', 'Изменение одной колонии не влияет на другую.'),
('Последовательности операций', 'Все 256 последовательностей длины 8 для M = 0, 1, 2, 3.'),
('Пустая корзина', 'Пустой список и пустое строковое представление.'),
('Два ядовитых названия', 'True для обоих; отказ без добавления.'),
('Допустимые названия', 'False; добавление без сообщений; порядок и разделитель.'),
('Независимость корзин', 'Разные списки; содержимое каждой сохраняется отдельно.'),
('Повторяющиеся грибы', 'Оба экземпляра одного названия учитываются.'),
('Отказ после добавления', 'Ядовитый гриб не меняет уже заполненную корзину.')])
para('В переборной проверке выполнено 4 × 256 = 1024 сценария, всего 8192 операции. После каждого шага фактический счётчик сравнивается с эталоном: min(M, n + 1) для добавления и max(0, n − 1) для удаления. Это проверяет возврат к допустимым операциям после отказа, многократные отказы и чередование действий.')
verification = (ROOT / 'docs/verification.txt').read_text()
summary = verification[verification.index('----------------------------------------------------------------------'):].strip()
code(summary, 'Листинг 11 — итог фактического запуска unittest')
para('Демонстрационные программы также завершились с кодом 0. Требования задания выполнены: границы численности соблюдены, ошибки вывода и общего списка устранены, строковое представление корзины соответствует образцу.')

heading('8. Ответы по теории, вывод и источники')
para('В PDF отдельный перечень контрольных вопросов не приведён. Ниже раскрыты понятия из раздела «Что необходимо знать для выполнения работы».')
sub('Чем класс отличается от объекта?')
para('Класс описывает набор атрибутов и методов. Объект — конкретный экземпляр этого описания. Один класс MushroomsCollector позволяет создать несколько корзин, каждая со своим содержимым.')
sub('Для чего нужен инициализатор?')
para('Метод __init__ автоматически вызывается для инициализации экземпляра. Он сохраняет переданные параметры и задаёт начальное состояние: вместимость, нулевой счётчик или пустой список.')
sub('Как связаны метод объекта и self?')
para('Метод работает с конкретным экземпляром. При вызове colony.create() Python передаёт colony первым аргументом self. Поэтому self.current_bacteria_count относится к счётчику именно этой колонии.')
sub('Чем отличаются атрибуты класса и объекта?')
para('Атрибут класса определяется в теле класса и доступен экземплярам через поиск атрибутов. Атрибут экземпляра создаётся присваиванием self.name. Общий изменяемый список неудобен для независимых корзин: его изменение видно всем объектам, использующим этот список.')
sub('Вывод')
para('Выполнены оба задания лабораторной работы. Реализован класс BacteriaProducer с ограничением численности и сообщениями об операциях. Исправлен и дополнен MushroomsCollector: каждая корзина хранит собственные данные, ядовитые названия отклоняются, содержимое выводится через запятую и пробел. Освоены инициализация, методы экземпляра, логические проверки и метод __str__. Работоспособность подтверждена исходными примерами и 12 автоматическими тестами, включая граничные состояния и независимость объектов.')
sub('Использованные материалы')
para('1. ДГТУ. Лабораторная работа № 2 «Введение в объектно-ориентированное программирование. Классы и объекты». Предоставленный файл «Лабораторная_работа_2.pdf», с. 1–3.')
para('2. Исходные учебные файлы main.py и task2.py, предоставленные вместе с заданием.')
para('3. Ковалев Д. П. Отчёт по лабораторной работе № 1, группа ВКБ53. Использован как образец титульного листа и оформления.')

compact = False
for paragraph in doc.paragraphs:
    if paragraph.text.startswith('7. Автоматическая'):
        compact = True
    if compact:
        paragraph.paragraph_format.line_spacing = 1
        paragraph.paragraph_format.space_after = Pt(4)
        if paragraph.style.name == 'Heading 2':
            paragraph.paragraph_format.space_before = Pt(6)
doc.save(ROOT / 'docs/Ковалев Д.П. ВКБ53 ЛР2.docx')
print(ROOT / 'docs/Ковалев Д.П. ВКБ53 ЛР2.docx')
