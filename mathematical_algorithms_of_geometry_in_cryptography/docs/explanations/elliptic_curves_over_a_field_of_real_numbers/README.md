# Эллиптические кривые (ЭК) над полем вещественных чисел

> [!IMPORTANT]
> Перейдите в директорию с файлом `cli.py` в терминале. `cd src/mathematical_algorithms_of_geometry_in_cryptography`

```bash
python cli.py elliptic-curve generate -a 1 -b 2
```