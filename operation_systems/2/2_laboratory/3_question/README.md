## Подсчитайте количество строк, в которых содержится пользователь `student`(любой, кроме `root`) в файле `/etc/group`

Введите команду 

```bash
grep “student” /etc/group | grep –v “root” | wc –l
```

> [!MOTE]
> `wc -l` - подсчёт строк
> `-v` - означает НЕ
