## Подсчитайте количество строк, в которых НЕ содержится пользователь `student`(любой, кроме `root`)  в файле `/etc/group`

Воспользуйтесь командой, которая представлена ниже: 

```bash
grep -v “student” /etc/group | grep –v “root” | wc –l
```


