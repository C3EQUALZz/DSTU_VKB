## Используя команду `grep`, отобразите все строки, содержащие слово `List` или слово `System` в файле `/etc/passwd`

Воспользуйтесь командой, которая представлена ниже: 

```bash
grep “List\|System” /etc/passwd
```
