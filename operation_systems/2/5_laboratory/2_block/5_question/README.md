## Создайте директорию `/data`, в которую вы будете монтировать новый логический том `/dev/sdc2`.

Для создания директории, можете воспользоваться командой, которая представлена ниже: 

```bash
sudo mkdir /data
```

