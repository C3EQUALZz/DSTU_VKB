sonar.projectKey=lab_01_rsa
sonar.projectName=Лабораторная № 1 — RSA
sonar.projectVersion=0.1.0

sonar.sources=src
sonar.tests=tests
sonar.exclusions=**/target/**
sonar.sourceEncoding=UTF-8

# Rust (если установлен community-rust плагин).
sonar.language=rust
# Импорт замечаний Clippy (генерируется через `cargo clippy --message-format=json`).
sonar.community.rust.clippy.reportPaths=clippy.json
# Импорт code coverage (если когда-нибудь подключим cargo-llvm-cov / tarpaulin).
# sonar.community.rust.lcov.reportPaths=lcov.info
