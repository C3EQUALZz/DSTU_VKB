# Аудит файлового сервера /srv/ftp — пункт 17 рабочего задания.
# Любое чтение/запись/изменение метаданных файлов под этим каталогом
# попадает в /var/log/audit/audit.log с тегом ftp-access.

-w /srv/ftp -p rwxa -k ftp-access
